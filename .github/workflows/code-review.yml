name: Code Review with LLM

on:
  pull_request:
    branches: [ main ]

jobs:
  code-review:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        ref: ${{ github.event.pull_request.head.ref }}
    
    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        
    - name: Get changed files
      id: changed-files
      uses: tj-actions/changed-files@v44
      with:
        files: |
          **/*.py
          **/*.js
          **/*.ts
          **/*.java
          **/*.cpp
          **/*.c
          **/*.go
          **/*.rs
          **/*.rb
          **/*.php
        files_ignore: |
          **/*.test.*
          **/*.spec.*
          **/test/**
          **/tests/**
          **/__tests__/**
    
    # - name: Generate code review
    #   id: review
    #   if: steps.changed-files.outputs.any_changed == 'true'
    #   env:
    #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    #     LLM_API_URL: ${{ secrets.LLM_API_URL || 'http://localhost:8000/api/review/' }}
    #     PR_NUMBER: ${{ github.event.pull_request.number }}
    #   run: |
    #     python .github/scripts/code_review.py
        
    # - name: Post review as comment
    #   if: steps.changed-files.outputs.any_changed == 'true' && steps.review.outputs.has_review == 'true'
    #   uses: actions/github-script@v6
    #   with:
    #     script: |
    #       const reviewContent = `## 🤖 AI Code Review\n\n${process.env.REVIEW_RESULT}`;
          
    #       // Xóa comment cũ nếu có
    #       const { data: comments } = await github.rest.issues.listComments({
    #         owner: context.repo.owner,
    #         repo: context.repo.repo,
    #         issue_number: ${{ github.event.pull_request.number }}
    #       });
          
    #       const botComments = comments.filter(comment => 
    #         comment.body.includes('🤖 AI Code Review') || 
    #         comment.user.login === 'github-actions[bot]'
    #       );
          
    #       for (const comment of botComments) {
    #         await github.rest.issues.deleteComment({
    #           owner: context.repo.owner,
    #           repo: context.repo.repo,
    #           comment_id: comment.id
    #         });
    #       }
          
    #       // Tạo comment mới
    #       await github.rest.issues.createComment({
    #         owner: context.repo.owner,
    #         repo: context.repo.repo,
    #         issue_number: ${{ github.event.pull_request.number }},
    #         body: reviewContent
    #       });
    #   env:
    #     REVIEW_RESULT: ${{ steps.review.outputs.review_content }}
    
    # - name: Skip if no code files changed
    #   if: steps.changed-files.outputs.any_changed == 'false'
    #   run: echo "No code files changed. Skipping code review."